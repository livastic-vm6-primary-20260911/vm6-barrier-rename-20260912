pull_request_rules: []
commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: restricted
    queue_conditions:
      - label = vm6-explicit-queue-gate
    merge_conditions: []
    merge_method: merge
    checks_timeout: null
