pull_request_rules:
  - name: VM6 updates synchronize ordering probe 20260920
    conditions:
      - "#updates = 0"
      - files = vm6-updates-race-trigger-20260920.txt
    actions:
      comment:
        message: "VM6_UPDATES_RACE_PROBE_20260920"
commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  mode: serial
  max_parallel_checks: 1
queue_rules:
  - name: default
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = @github-actions/vm6-synthetic-head-delay
    max_checks_retries: 0
    merge_method: merge
    checks_timeout: null
