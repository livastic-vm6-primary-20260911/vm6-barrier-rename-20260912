pull_request_rules: []
commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  max_parallel_checks: 1
queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - deployment-success = vm6-inflight-deploy
      - check-success = @github-actions/vm6-inflight-ci
    merge_method: merge
    checks_timeout: null
