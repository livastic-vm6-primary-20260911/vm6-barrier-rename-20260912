pull_request_rules:
  - name: VM6 reset external merge freshness R9V4
    conditions:
      - base = vm6-reset-base-r9v4
      - title = VM6 reset external merge freshness R9V4
    actions:
      queue:
        name: vm6-reset-r9v4
commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  mode: serial
  max_parallel_checks: 1
  reset_on_external_merge: never
queue_rules:
  - name: vm6-reset-r9v4
    batch_size: 1
    queue_conditions: []
    merge_conditions:
      - check-success = @github-actions/vm6-synthetic-head-delay
    max_checks_retries: 0
    merge_method: merge
    checks_timeout: null
