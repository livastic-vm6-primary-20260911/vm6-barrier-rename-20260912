pull_request_rules:
  - name: vm6-wa-classic-linear-history
    conditions:
      - base = vm6-classic-linear-history-target
      - title = VM6 WA CLASSIC LINEAR
      - body = VM6_LINEAR_RELEASE
    actions:
      merge:
        method: merge
        merge_bot_account: livastic-vm6-primary-20260911
commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
merge_queue:
  max_parallel_checks: 2
scopes:
  source:
    files:
      src:
        include:
          - src/**
      docs:
        include:
          - docs/**
  barrier_files:
    include:
      - barrier/**
queue_rules:
  - name: default
    queue_conditions: []
